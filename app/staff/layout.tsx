import { StaffLayout } from "@/components/staff/StaffLayout";

export default function StaffLayoutRoot({ children }: { children: React.ReactNode }) {
  return <StaffLayout>{children}</StaffLayout>;
}
